<div class="p-4 card bg-gradient-to-r bg-gradient-to-bl from-pink-400 via-red-400 to-yellow-400 text-gray-900 ">
    
    <div class="-mt-12 flex justify-center">
        <div class="bg-gray-900 p-2 rounded-xl text-gray-50">{{ service.data.icon | safe }}</div>
        
    </div>
    <p class="mt-2 text-center font-semibold">{{ service.data.title }}</p> 

    <div class="mt-2">
        <p>{{ service.data.content | safe }} </p>
    
    </div>
    
    
</div> 