<div class="rounded shadow p-4 bg-white {% if forloop.index == 5 %}col-span-1 sm:col-span-2{% endif %}">
    
    <div class="-mt-12 flex justify-center">
        <img class="h-16 w-16 shadow ring-black ring-4 rounded-full" src="{{ testimonial.data.img }}" alt="{{ testimonial.data.reviewer }}'s Photo">
    </div>

    <div class="mt-6">
        <p>{{ testimonial.data.content | safe }} </p>
        <p class="font-semibold">{{ testimonial.data.reviewer }} {% if testimonial.data.client %}, {{ testimonial.data.client }}{% endif %}</p>
    </div>
    
    
</div>