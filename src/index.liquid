---
layout: layouts/wrapper.liquid

title: Home
description: Maciej Janowski Freelance full-stack developer
---

<div class="container">
    {% include 'components/hero' %}
</div>


<div class="container mt-32 grid grid-cols-1 sm:grid-cols-2 gap-16">
    {% assign testimonials = collections.testimonials | order %}
        {% for testimonial in testimonials %}
            {% include 'components/testimonial' %}
        {% endfor %}
</div>

<div class="mt-32">
    <div class="">
        <div id="read" class="container">
            {% include 'blocks/content' %}
        </div>
        <div class="container mt-32">
            {% include 'components/about' %}
        </div>

        <div class="container mt-32">
            {% include 'blocks/content2' %}
        </div>

        <div class="container mt-32">
            {% include 'blocks/cta' %}
        </div>

    </div>
</div>